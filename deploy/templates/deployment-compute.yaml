apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ui.computeName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ui.labels" . | nindent 4 }}
    {{- include "ui.computeSelectorLabels" . | nindent 4 }}
spec:
  {{- if not .Values.compute.autoscaling.enabled }}
  replicas: {{ .Values.compute.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ui.computeSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ui.labels" . | nindent 8 }}
        {{- include "ui.computeSelectorLabels" . | nindent 8 }}
      annotations:
        checksum/nginx: {{ include (print $.Template.BasePath "/configmap-nginx.yaml") . | sha256sum }}
    spec:
      enableServiceLinks: false
      volumes:
        - name: nginx-template
          configMap:
            name: {{ include "ui.computeName" . }}-nginx
            items:
              - key: default.conf.template
                path: default.conf.template
      containers:
        - name: compute
          image: {{ include "ui.computeImage" . | quote }}
          imagePullPolicy: {{ include "ui.computeImagePullPolicy" . }}
          ports:
            - name: http
              containerPort: {{ .Values.compute.port }}
          volumeMounts:
            - name: nginx-template
              mountPath: /etc/nginx/templates/default.conf.template
              subPath: default.conf.template
              readOnly: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            {{- toYaml .Values.compute.resources | nindent 12 }}
