apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ui.iamName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ui.labels" . | nindent 4 }}
    {{- include "ui.iamSelectorLabels" . | nindent 4 }}
spec:
  {{- if not .Values.iam.autoscaling.enabled }}
  replicas: {{ .Values.iam.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ui.iamSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ui.labels" . | nindent 8 }}
        {{- include "ui.iamSelectorLabels" . | nindent 8 }}
      annotations:
        checksum/nginx: {{ include (print $.Template.BasePath "/configmap-nginx.yaml") . | sha256sum }}
    spec:
      enableServiceLinks: false
      volumes:
        - name: nginx-template
          configMap:
            name: {{ include "ui.iamName" . }}-nginx
            items:
              - key: default.conf.template
                path: default.conf.template
      containers:
        - name: iam
          image: {{ include "ui.iamImage" . | quote }}
          imagePullPolicy: {{ include "ui.iamImagePullPolicy" . }}
          ports:
            - name: http
              containerPort: {{ .Values.iam.port }}
          volumeMounts:
            - name: nginx-template
              mountPath: /etc/nginx/templates/default.conf.template
              subPath: default.conf.template
              readOnly: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            {{- toYaml .Values.iam.resources | nindent 12 }}
