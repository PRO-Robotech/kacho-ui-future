apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ui.systemName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ui.labels" . | nindent 4 }}
    {{- include "ui.systemSelectorLabels" . | nindent 4 }}
spec:
  {{- if not .Values.system.autoscaling.enabled }}
  replicas: {{ .Values.system.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ui.systemSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ui.labels" . | nindent 8 }}
        {{- include "ui.systemSelectorLabels" . | nindent 8 }}
      annotations:
        checksum/nginx: {{ include (print $.Template.BasePath "/configmap-nginx.yaml") . | sha256sum }}
    spec:
      enableServiceLinks: false
      volumes:
        - name: nginx-template
          configMap:
            name: {{ include "ui.systemName" . }}-nginx
            items:
              - key: default.conf.template
                path: default.conf.template
      containers:
        - name: system
          image: {{ include "ui.systemImage" . | quote }}
          imagePullPolicy: {{ include "ui.systemImagePullPolicy" . }}
          ports:
            - name: http
              containerPort: {{ .Values.system.port }}
          volumeMounts:
            - name: nginx-template
              mountPath: /etc/nginx/templates/default.conf.template
              subPath: default.conf.template
              readOnly: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            {{- toYaml .Values.system.resources | nindent 12 }}
