{
  "name": "kacho-ui-future",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "vpc",
    "iam",
    "system"
  ],
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File ./dev-federation.ps1",
    "typecheck": "npm run typecheck --prefix host && npm run typecheck --prefix dashboard && npm run typecheck --prefix vpc && npm run typecheck --prefix compute && npm run typecheck --prefix storage && npm run typecheck --prefix nlb && npm run typecheck --prefix registry && npm run typecheck --prefix iam && npm run typecheck --prefix system",
    "test": "npm test --prefix host && npm test --prefix dashboard && npm test --prefix vpc && npm test --prefix compute && npm test --prefix storage && npm test --prefix nlb && npm test --prefix registry && npm test --prefix iam && npm test --prefix system"
  },
  "overrides": {
    "dompurify": "3.4.11"
  }
}
