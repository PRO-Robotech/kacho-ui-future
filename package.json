{
  "name": "kacho-ui-future",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File ./dev-federation.ps1",
    "typecheck": "npm run typecheck --prefix host && npm run typecheck --prefix dashboard && npm run typecheck --prefix vpc",
    "test": "npm test --prefix host && npm test --prefix dashboard && npm test --prefix vpc"
  }
}
